# Copy this file to .env.local and adjust values as needed.

# Backend API base URL (defaults to empty = same origin via Next.js rewrite)
# For local development with separate ports, leave this empty and use next.config.js rewrites.
NEXT_PUBLIC_API_URL=
